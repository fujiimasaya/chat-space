.chat-main
  .chat-main__group-info
    .chat-main__group-info__left-box
      .chat-main__group-info__left-box__current-group
        = @group.name
      .chat-main__group-info__left-box__member-list
        - @group.users.each do |user|
          = user.name
    .chat-main__group-info__edit-btn
      = link_to edit_group_path(@group.id) do
        = icon('fas', 'edit', class: 'send')


      -# class:"info" do
      -#   edit

        -# = link_to new_group_path do
            -# = icon('fas', 'edit', class: 'icon')

  .chat-main__message
    = render @messages

  .form
    = form_for [@group, @message] do |f|
      = f.text_field :content, class: 'form__message', placeholder: 'type a message'
      .form__mask
        = f.label :image, class: 'form__mask__image' do
          = icon('fas', 'image', class: 'icon')
          = f.file_field :image, class: 'hidden'
        = f.submit 'Send', class: 'form__submit'


  -# .chat-main__form
  -#   = form_for [@group, @message], html: {class:"chat-main__form__data"} do |f|
  -#     .chat-main__form__data__input-box
  -#       .chat-main__form__data__input-box__new-message
  -#         = f.text_field :content, class: 'form__message', placeholder: 'type a message'
  -#     .chat-main__form__data__input-box__new-image
  -#       = f.label :image, class: 'form__mask__image' do
  -#         = icon('fas', 'image', class: 'icon')
  -#         = f.file_field :image, class: 'hidden'
  -#     = f.submit 'Send', class: 'form__submit'

